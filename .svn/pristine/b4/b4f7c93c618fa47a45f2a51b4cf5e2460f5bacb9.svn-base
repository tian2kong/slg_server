local class = require "class"

local IPlayerModule = require "iplayermodule"

local PlayerArmyModule = class("PlayerArmyModule", IPlayerModule)

local s_table = {
    table_name = "plyaer_army",
    key_name = "playerid",
    field_name = {
    	"freequeue", --闲置部队
    	"curequeue", --治疗中部队 
    	"trainqueue", --训练中部队
    	"injuredqueue", --受伤的部队
    },
	
}

local 


function PlayerArmyModule:ctor(player)

end

return PlayerArmyModule